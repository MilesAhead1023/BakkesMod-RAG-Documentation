# Optional dependencies for advanced features.
# Install individual groups as needed.
#
# Observability (Phoenix tracing + Prometheus metrics):
#   pip install arize-phoenix openinference-instrumentation-llama-index prometheus-client opentelemetry-api
#
# RAG evaluation (RAGAS):
#   pip install ragas datasets
#
# Distributed cache (Redis):
#   pip install redis
#
# ColBERT reranking:
#   pip install ragatouille
#
# REST API server:
#   pip install fastapi uvicorn httpx
#
# Advanced neural reranking (BGE, FlashRank, Cohere):
#   pip install llama-index-postprocessor-cohere-rerank llama-index-postprocessor-flag-embedding-reranker llama-index-postprocessor-flashrank-rerank FlagEmbedding cohere
#
# C++ syntax analysis (tree-sitter AST, optional — falls back to regex):
#   pip install tree-sitter-language-pack
#
# MCP server (Claude Code IDE integration):
#   pip install mcp

# Observability & monitoring
arize-phoenix>=4.0.0
openinference-instrumentation-llama-index>=2.0.0
prometheus-client>=0.19.0
opentelemetry-api>=1.20.0

# Evaluation & quality assurance
ragas>=0.1.0
datasets>=2.0.0

# Distributed cache
redis>=5.0.0

# Advanced reranking (ColBERT)
ragatouille>=0.0.8

# REST API server
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.25.0

# Advanced neural reranking (all guarded by try/except ImportError in retrieval.py)
llama-index-postprocessor-cohere-rerank>=0.1.0
llama-index-postprocessor-flag-embedding-reranker>=0.1.0
llama-index-postprocessor-flashrank-rerank>=0.1.0
FlagEmbedding>=1.3.0
cohere>=5.0.0

# C++ syntax analysis (tree-sitter AST, optional — falls back to regex)
tree-sitter-language-pack>=0.6.0

# MCP server (Claude Code IDE integration)
mcp>=0.1.0
